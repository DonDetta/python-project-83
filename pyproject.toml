[project]
name = "hexlet-code"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "gunicorn>=21.0",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "flake8>=7.0",
]